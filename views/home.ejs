<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    
    <title>Drop</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles_home.css" />
    
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto+Mono&display=swap"
      rel="stylesheet"
    />
    <script src="https://kit.fontawesome.com/183d6b54f5.js" crossorigin="anonymous"></script>

  </head>
  <body>
    <% if(user.devices == ""){%> 
      <div class="p-5 mb-4 bg-light rounded-3">
        <div class="container-fluid py-5">
          <h1 class="display-5 fw-bold">You don't have devices.</h1>
          <p class="col-md-8 fs-4">Please contact the support through the button bellow in order to adquire one.</p>
          <button onclick="location.href='mailto:lucashnriquer@gmail.com';" class="btn btn-info btn-lg" type="submit">Get in touch!</button>
        </div>
      </div>
    <%} else{%> 
      <h1 class="h1_header">Your Devices</h1>
      <%user.devices.forEach((device)=>{%>
        <div class="devices">
          <p class="id"># <%= device.id %></p>
          <h2 class="p-devices"><%= device.name %></h2>
          <h2 class="p-devices data"><%= device.led_status %></h2>
          <form  class="btn-form" action="/button/<%= user.username %>/<%= device.id %> " method="post">
            <button class="btn_me" name="led_status" value="<%= !device.led_status %>" type="submit">
              <i class="fa-solid fa-arrows-rotate fa-2x"></i>
            </button>
          </form>
          <h2 class="p-devices data"><%= device.data %></h2>
          <form  class="btn-form" action="/config/<%= device.id %> " method="get">
            <button class="btn_me"  type="submit">
              <i class="fa-solid fa-gear fa-2x"></i>
            </button>
          </form>
        </div>
      <%})%>
      <div class="btn_end">
        <button onclick="location.href='mailto:lucashnriquer@gmail.com';" class="btn btn-info btn-lg" type="submit">Get more devices!</button>
      </div>
    <%}%>
  </body>
</html>
